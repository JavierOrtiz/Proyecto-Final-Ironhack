<div class="container well">
    <div class="row">
        <h2>Filtrado de grupos</h2>
        <div class="col-lg-12">
            <input type="search" class="form-control" id="input-search" placeholder="¿Que buscas?" >
            <hr>
        </div>
        <div class="searchable-container">
            <% @groups.each do |user| %>
                <div class="items col-xs-12 col-sm-6 col-md-6 col-lg-6 clearfix">
                    <div class="info-block block-info clearfix">
                        <div class="square-box pull-left">
                            <span class="fa fa-user fa-5x"></span>
                        </div>
                        <h5>ID: <%= user.invitationHash %></h5>
                        <h4>Jefe: <%= user.name %></h4>
                        <p>Empleados: <span class="label label-success"><%= user.employees.size %> <i class="fa fa-users"></i></span></p>
                        <span>Email: <%= user.email %></span><br>
                        <a href="#" class="btn btn-warning btn-xs"><i class="fa fa-eye"></i> Ver equipo</a>
                        <a href="#" class="btn btn-info btn-xs"><i class="fa fa-support"></i> Enviar mensaje</a>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</div>
